# 개발노트 — 재무제표 비교 제거 → 회계기준서 메뉴 신설

## 2026-06-23 작업 내역
- 작업 내용:
  - **재무제표 비교(숫자) 탭 전면 제거** 후, 그 자리에 **회계기준서** 메뉴 신설.
  - 회계기준서 = 4개사/기간과 무관한 **독립 문서공간**: 임의 PDF 업로드 → 본문 전체 인덱싱 → 검색 → 본문 출처(쪽수)·원문 PDF 표시. **검색전용**(AI 답변 없음)·인용강제·폐쇄망.
  - Ideation/스펙 승인 계획: `C:\Users\2hryu\.claude\plans\smooth-booping-hamming.md`.
- 변경 파일:
  - **삭제**: `src/fs_compare.py`, `tests/test_fs_compare.py`. `prism_fs.spec` hiddenimports에서 `fs_compare` 제거(+`logsetup` 추가).
  - **백엔드 `src/app.py`**: `import fs_compare`·`/api/fs/*` 8개 제거. `STANDARDS_ROOT`/`STANDARDS_CATALOG_PATH` 상수, 카탈로그 헬퍼 4종(`load/save/upsert/remove_standards`), `_slugify_standard`/`_standard_doc_id`(sha1 멱등), 경로 헬퍼, `_index_standard`(build_body_index 재사용), `/api/standards/{upload,list,index,index/status,search,pdf,delete}` 7개. `build_body_index`에 `out_path` 인자 1개 추가(기존 호출 무영향). `import hashlib`.
  - **`src/paths.py`**: `STANDARDS_ROOT = STORAGE_ROOT / "accounting_standards"`.
  - **프런트 `src/static/index.html`**: 재무제표 탭/JS(258줄)/CSS/provenance 팝업 제거. `#tab-standards` 패널(업로드 폼·기준서 목록·검색·미리보기), 회계기준서 JS(`loadStandards`/`uploadStandard`/`indexStandard`/`pollStandardIndex`/`deleteStandard`/`doStandardSearch`/`stdPreview`), `showTab` 갱신. `renderPdfRange`에 `urlOverride` 인자 추가(회사/기간 모델 외 PDF 소스용) — 형광·더블클릭 새창 100% 재사용.
  - **테스트**: `tests/test_standards.py`(신규 5: doc_id 멱등·traversal, 카탈로그 격리, 경로, 인덱싱(fs_div=all·B유닛), 검색 라우팅). `tests/e2e/test_smoke.py` fs→standards 갱신.
  - `VERSION` 0.12.1 → 0.13.0.
- 결정 사항:
  - 임의 PDF는 주석헤더 휴리스틱 없이 **본문 전체 인덱싱(`build_body_index`, fs_div="all")** 만으로 검색·출처·표시 충족. 신규 알고리즘 0건(기존 함수 재사용).
  - 재무수치(DART fnltt→fs_structured.json) 수집 로직은 **유지**(UI/엔드포인트만 제거).
  - 검색 범위: 전체 통합 + 목록 체크박스 선택. 검색전용(LLM 생성 없음).
  - doc_id = `{slug}_{sha1(content)[:8]}` — 중복 멱등·동명이내용 분리·traversal 차단. doc_id 화이트리스트로 PDF 서빙 traversal 방어.
  - 스니펫 보강: `_best_unit`은 본문 유닛 최상위가 첫 청크면 text=None(제목 임베딩 동점) → standards_search가 로드한 인덱스에서 매칭 페이지 청크로 직접 보강.
- 검증:
  - pytest **169 통과**(standards 5 신규, e2e 갱신). 회귀 0.
  - 백엔드 e2e(curl): 업로드→인덱싱(2s)→검색(retrieval_only, 랭킹)→PDF 200·traversal(`../../etc`) 404→삭제 정상.
  - UI(preview): 회계기준서 탭 렌더, 목록 인덱싱 배지(N청크), 검색 3건+스니펫, **PDF 미리보기 캔버스 렌더(`/api/standards/pdf`+urlOverride)**, 재무제표 탭 제거 확인.
- 미완료/다음 작업:
  - 실제 회계기준서 PDF(정상 임베드 폰트)로 PDF 형광 하이라이트 최종 확인(합성 fitz PDF는 한글 텍스트레이어 한계로 마크 0).
  - setup v0.13.0 재빌드(회계기준서·파일로깅 포함).
- 이슈/특이사항:
  - 타이틀(4대 금융지주 …분석)은 주석 비교/검색/커버리지가 핵심이라 유지.
  - bash curl로 한글 파일명/제목 시드 시 멀티파트 인코딩 깨짐(테스트 아티팩트) — 브라우저 업로드는 정상.

## 2026-06-23 (2) 회계기준서 문단 단위 구조화
- 작업 내용:
  - K-IFRS 기준서는 **문단번호(1·한3.1·11A·B40·IG40E·BC72B)가 인용 단위** → 페이지 단위 대신 **문단 단위 구조화** 도입. 출처를 "문단 35H · 신용위험"으로 제시.
  - `extract_standard_paragraphs(pdf)` + 코어 `_segment_standard_lines(pages)`(PDF I/O 분리, 테스트 가능): 목차(목  차) 섹션 제목 화이트리스트 → 본문 섹션 태깅, 본문 앵커 단조증가 가드(base≤직전+3,1~60)로 표 안 숫자 오탐 차단, 부록B/IG/BC 접두 분할·구획 태깅, 표지·저작권·목차 자동 제외.
  - `build_standard_index`: 문단 파서 우선, 문단<10이면 **페이지 단위 폴백(structured=False)**. 유닛 스키마는 build_body_index와 동일(no=문단번호, title=섹션, fs_div="all") → retrieve·검색·PDF뷰어 무변경.
  - `_index_standard`가 `build_standard_index` 호출. 검색 응답에 `section`(문단 섹션) 추가. 프런트 칩에 `문단 {no} · {섹션}`(페이지 폴백은 `p.N`) 표시.
- 변경 파일: `src/app.py`(파서·빌더·검색 section), `src/static/index.html`(칩 표기), `tests/test_standards.py`(코어 2건 추가).
- 결정 사항: 버전 유지(0.13.0 — 동일 미릴리스 기능 보강). 신규 알고리즘 1개(문단 파서)+폴백.
- 검증(실제 PDF: K-IFRS 제1107호 금융상품 공시, 239쪽):
  - 문단 파서: **본문 173 + 부록B 66 + IG 44 + BC 281 = 564 유닛**, 표 숫자 38개 정상 드롭, 섹션 정확(목적/적용범위/신용위험/금융자산과 금융부채의 범주…).
  - 라이브: 업로드→**structured=True, 1050청크(7s)**→검색 "신용위험 기대신용손실"→**문단 35H·35M·35J·35F(신용위험, p38-41, 79%)**→PDF 미리보기 캔버스+**형광 33마크**(실제 PDF 정상 하이라이트 확인).
  - pytest **171 통과**.
- 미완료/다음 작업: 적용사례/BC 구획의 부록 미니목차 라인 일부가 저품질(소수) — 추후 부록-TOC 페이지 스킵으로 개선 여지. setup v0.13.0 재빌드.

## 2026-06-23 (3) 검색 — 공시 부분 우선 표시 + 자동 미리보기
- 작업 내용:
  - 키워드 검색 시 관련 기준서의 **공시(disclosure) 부분을 먼저** PDF로 보여주도록 검색 재정렬·자동 미리보기 추가. (사용자 키워드→문서번호 표를 수용 기준으로 삼음.)
  - **파일명 기반 분류** `_classify_standard(filename, title)`→공시/표시/기타. 업로드 시 `doc_class` 저장(구 업로드는 검색 시 즉시 파생, 하위호환).
  - **결정론 복합 재정렬**(standards_search): retrieve 후보 풀 확대(top_k≥40) → ①공시 기준서 ②문서명에 질의어 ③본문(공시 요구사항) > 부록B > IG > BC ④의미점수 순 정렬 → top_k. 응답에 `doc_class`·`part` 추가.
  - **자동 미리보기**: 검색 직후 1순위(공시 본문) 문단 PDF를 자동 렌더(`stdPreview(0)`)+매칭어 형광. 칩에 [공시]/[표시] 배지·문단·섹션 표기, 1순위 강조.
- 변경 파일: `src/app.py`(`_classify_standard`·업로드 doc_class·검색 재정렬), `src/static/index.html`(자동 미리보기·배지·선택강조), `tests/test_standards.py`(분류·공시/본문 우선 정렬 2건 추가).
- 결정 사항: 버전 0.13.0 유지(동일 미릴리스 회계기준서 기능 보강). 키워드→문서 하드코딩 맵 불채택 — 내용검색이 이미 정확 라우팅(표는 수용 기준·검증용).
- 검증:
  - pytest **173 통과**. 라이브(실제 3개 기준서): 위험회피→1107·관계기업→1112·특수관계자→1024 모두 **top=[공시/본문]**. UI: 검색 즉시 공시 본문 PDF **자동 미리보기**+형광 28마크, [공시] 배지 표기 확인.
  - 함정: 8021에 구 서버가 점유 중이면 새 코드 미반영(doc_class/part=None) — 재시작으로 해소. 서버 기동 시간(~5s, 모델 로드)으로 fresh 여부 판별 가능.
- 미완료: 번들 3개는 모두 공시 → 공시 우선의 가시 효과는 표시 기준서(예 제1032호) 추가 시 뚜렷. setup 재빌드 시 반영.
