# 개발노트 — 재무제표 비교 제거 → 회계기준서 메뉴 신설

## 2026-06-23 작업 내역
- 작업 내용:
  - **재무제표 비교(숫자) 탭 전면 제거** 후, 그 자리에 **회계기준서** 메뉴 신설.
  - 회계기준서 = 4개사/기간과 무관한 **독립 문서공간**: 임의 PDF 업로드 → 본문 전체 인덱싱 → 검색 → 본문 출처(쪽수)·원문 PDF 표시. **검색전용**(AI 답변 없음)·인용강제·폐쇄망.
  - Ideation/스펙 승인 계획: `C:\Users\2hryu\.claude\plans\smooth-booping-hamming.md`.
- 변경 파일:
  - **삭제**: `src/fs_compare.py`, `tests/test_fs_compare.py`. `prism_fs.spec` hiddenimports에서 `fs_compare` 제거(+`logsetup` 추가).
  - **백엔드 `src/app.py`**: `import fs_compare`·`/api/fs/*` 8개 제거. `STANDARDS_ROOT`/`STANDARDS_CATALOG_PATH` 상수, 카탈로그 헬퍼 4종(`load/save/upsert/remove_standards`), `_slugify_standard`/`_standard_doc_id`(sha1 멱등), 경로 헬퍼, `_index_standard`(build_body_index 재사용), `/api/standards/{upload,list,index,index/status,search,pdf,delete}` 7개. `build_body_index`에 `out_path` 인자 1개 추가(기존 호출 무영향). `import hashlib`.
  - **`src/paths.py`**: `STANDARDS_ROOT = STORAGE_ROOT / "accounting_standards"`.
  - **프런트 `src/static/index.html`**: 재무제표 탭/JS(258줄)/CSS/provenance 팝업 제거. `#tab-standards` 패널(업로드 폼·기준서 목록·검색·미리보기), 회계기준서 JS(`loadStandards`/`uploadStandard`/`indexStandard`/`pollStandardIndex`/`deleteStandard`/`doStandardSearch`/`stdPreview`), `showTab` 갱신. `renderPdfRange`에 `urlOverride` 인자 추가(회사/기간 모델 외 PDF 소스용) — 형광·더블클릭 새창 100% 재사용.
  - **테스트**: `tests/test_standards.py`(신규 5: doc_id 멱등·traversal, 카탈로그 격리, 경로, 인덱싱(fs_div=all·B유닛), 검색 라우팅). `tests/e2e/test_smoke.py` fs→standards 갱신.
  - `VERSION` 0.12.1 → 0.13.0.
- 결정 사항:
  - 임의 PDF는 주석헤더 휴리스틱 없이 **본문 전체 인덱싱(`build_body_index`, fs_div="all")** 만으로 검색·출처·표시 충족. 신규 알고리즘 0건(기존 함수 재사용).
  - 재무수치(DART fnltt→fs_structured.json) 수집 로직은 **유지**(UI/엔드포인트만 제거).
  - 검색 범위: 전체 통합 + 목록 체크박스 선택. 검색전용(LLM 생성 없음).
  - doc_id = `{slug}_{sha1(content)[:8]}` — 중복 멱등·동명이내용 분리·traversal 차단. doc_id 화이트리스트로 PDF 서빙 traversal 방어.
  - 스니펫 보강: `_best_unit`은 본문 유닛 최상위가 첫 청크면 text=None(제목 임베딩 동점) → standards_search가 로드한 인덱스에서 매칭 페이지 청크로 직접 보강.
- 검증:
  - pytest **169 통과**(standards 5 신규, e2e 갱신). 회귀 0.
  - 백엔드 e2e(curl): 업로드→인덱싱(2s)→검색(retrieval_only, 랭킹)→PDF 200·traversal(`../../etc`) 404→삭제 정상.
  - UI(preview): 회계기준서 탭 렌더, 목록 인덱싱 배지(N청크), 검색 3건+스니펫, **PDF 미리보기 캔버스 렌더(`/api/standards/pdf`+urlOverride)**, 재무제표 탭 제거 확인.
- 미완료/다음 작업:
  - 실제 회계기준서 PDF(정상 임베드 폰트)로 PDF 형광 하이라이트 최종 확인(합성 fitz PDF는 한글 텍스트레이어 한계로 마크 0).
  - setup v0.13.0 재빌드(회계기준서·파일로깅 포함).
- 이슈/특이사항:
  - 타이틀(4대 금융지주 …분석)은 주석 비교/검색/커버리지가 핵심이라 유지.
  - bash curl로 한글 파일명/제목 시드 시 멀티파트 인코딩 깨짐(테스트 아티팩트) — 브라우저 업로드는 정상.
