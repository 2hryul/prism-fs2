# 개발노트 — 셋업파일 빌드 (v0.2.0)

## 2026-06-10 10:46 작업 내역

- **작업 내용**: 릴리스 셋업파일 빌드(PyInstaller onedir 풀번들). 6/4 빌드(v0.1.0) 이후 누적된 6/8 기능 추가분 반영을 위한 재빌드.
- **버전 변경**: `0.1.0 → 0.2.0` (MINOR 올림).
  - 사유: 6/4 빌드 이후 하위호환 **기능 추가** 다수 — 자료추가 모달 팝업, 문서별 삭제, 업로드 PDF 자동감지, 온톨로지 Graph-RAG 검색 통합, 별도검토보고서(review_sep) 문서축 추가. CLAUDE.md 버전 규칙(기능 추가 → MINOR) 적용.
- **변경 파일**: `VERSION`(0.1.0→0.2.0).
- **빌드 절차**: `.\build.ps1` (PRISM_VERSION 자동 주입).
  1. pytest 게이트 — **109 passed / 4 skipped** 통과.
  2. 외부 https=0 게이트(폐쇄망 회귀) — 통과(w3.org/json-schema.org만 허용).
  3. PyInstaller `prism_fs.spec` onedir → `dist\setup\setup_v0.2.0\`.
  4. `src\storage`(데모 데이터) exe 옆 복사(.env 제외).
- **산출물**:
  - `dist\setup\setup_v0.2.0\setup_v0.2.0.exe` (런처 87.6 MB)
  - `_internal\`(torch + ko-sroberta 모델 + 정적 UI 동봉), `storage\`(데모 데이터)
  - 번들 전체 약 **6.5 GB**.
- **검증**:
  - 빌드 종료 코드 0, COLLECT 정상 완료.
  - 이전 `setup_v0.1.0` 삭제 후 재생성 확인(dist clean).
  - 산출물 파일명 ↔ VERSION 일치(setup_v0.2.0).
- **결정/특이사항**:
  - onedir 풀번들 유지(폐쇄망에서 임베딩 모델 외부 다운로드 0 보장, 768차원 인덱스 정합).
  - PyInstaller 로그의 transformers/torch 서브모듈 수집 WARNING(einops/tensorboard/dask 미설치)은 선택적 의존성 — 기동 무영향.
- **미완료/다음 작업**:
  - frozen exe 실기동 스모크 테스트(서버 기동 + 브라우저 오픈) 미수행 — 데모 PC 반입 전 1회 확인 권장.
  - 인스톨러(단일 setup.exe, Inno Setup/MSI) 형태는 미적용 — 현재는 onedir 폴더 배포. 단일 인스톨러 필요 시 별도 작업.
  - (이월) annual(FY 사업보고서) 본문 표시 PDF 자동 다운로드 실패 — 수동 업로드로 대응.
