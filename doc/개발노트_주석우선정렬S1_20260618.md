# 개발노트 — 주석 우선 정렬(S1) · 검색 정확도 회복

## 2026-06-18 14:10 작업 내역
- 작업 내용:
  - 본문 전체 인덱싱(`no="B…"` 유닛) 도입 이후 주석 제목검색이 본문 페이지 유닛에 밀리는 희석 발생(골든셋 n=25, MRR 0.853→0.667, 8/25에서 본문이 정답 주석보다 1위).
  - **S1 — 주석 우선 정렬**: `notes_rag.retrieve` 정렬 키에서 본문 유닛(`note_no` 가 `"B"` 시작)에만 `BODY_RANK_PENALTY=0.15` 차감. 표시 `score` 는 불변(정렬 키에만 적용) → provenance·UI 일관 유지.
  - 결과 MRR 0.667 → 0.813 회복. 본문 단독질의(예: 감사의견)는 여전히 상위 유지.
  - 회귀 테스트 추가: `test_note_beats_body_unit_on_tie`(동점 시 주석 우선 + 표시 score 동점 보존).
- 변경 파일:
  - `src/notes_rag.py` (상수 `BODY_RANK_PENALTY`, 정렬 키)
  - `tests/test_notes_rag_chunks.py` (회귀 테스트 1건)
  - `VERSION` 0.11.1 → 0.11.2
- 결정 사항:
  - 버전: PATCH 0.11.2(검색 정렬 품질 개선, 하위호환 유지).
  - 표시 점수는 건드리지 않고 정렬 키에만 패널티 → UI/근거표시 영향 0.
- 미완료/다음 작업:
  - 셋업 재빌드: src/storage rescan(24셀) 후 `setup_v0.11.2` 재빌드 진행 중.

## 2026-06-18 14:55 작업 내역 (S2 — 측정 하니스 보완)
- 작업 내용:
  - `scripts/eval_search.py` 의 `fs_div`→`doc_type` 라우팅 버그 수정. 별도재무제표 골든질의(`fs_div="별도"`)가 연결 인덱스(`doc_type="review"`)에서만 검색돼 과소평가되던 문제. `별도→review_sep` 유도(항목 명시 `doc_type` 우선) + 파라미터 전달.
  - **엔진 무관, 측정도구만 수정** — 실 UI(`cell_keys`/`doc_type`)는 이미 별도 정상 조회.
  - 측정 결과: 전체 MRR 0.813→**0.893**, hit@1 0.84, 별도 카테고리 MRR 1.0. (서버 :8021, 골든셋 n=25)
- 변경 파일: `scripts/eval_search.py`
- 결정 사항: 측정도구 한정 변경이므로 VERSION 0.11.2 유지(엔진/셋업 영향 없음).
- 이슈/특이사항:
  - pytest 160 passed(69s) — 작업트리 상태 그대로 통과 확인.
  - VERSION 은 빌드타임(`build.ps1`/`prism_fs.spec`)에서만 소비, 소스 하드코딩 없음. 헤더 `v0.1.0`(static/index.html)은 별개 기존 이슈.
