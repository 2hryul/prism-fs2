# 개발노트 — 자료추가 모달 DART 수집 상세내역·문서구분·등록현황 개선

## 2026-06-12 작업 내역

- **작업 내용**: "분기/연간 자료 추가" 모달 UX 3건 개선.
  1. **수집 상세내역 표시** — 완료 시 한 줄 배너 대신 상세 블록: 원본 보고서명(report_nm)+접수번호, 문서별(본문/별도검토/연결검토) 원본 파일명·저장 파일명(변경 여부 표시)·인덱싱 주석 수, 재무데이터(XBRL) 연결/별도 수집 여부, 저장경로, 라이브러리 등록 확인.
  2. **문서 구분 명확화** — 본문은 기간 인지 라벨(`reportDocLabel`: FY=사업보고서/Q2=반기/Q1·Q3=분기), 검토 2종은 "🏢 별도재무제표 검토보고서"/"🔗 연결재무제표 검토보고서"로 표기. 재무데이터는 CFS(연결)/OFS(별도) 분리 표시.
  3. **등록 현황 가시성** — 4사 일괄 수집 시 회사별 결과를 누적 표시(기존: 다음 회사 진행 메시지가 덮어씀), 인덱싱 실패(error) 시에도 `loadLibrary()` 호출(수집 단계에서 카탈로그 등록은 이미 완료된 상태), `closeAddModal()` 시 매트릭스 즉시 갱신(모달이 매트릭스를 가리는 문제 보완).
- **구현**:
  - `src/app.py`: `_doc_detail()`/`_collect_details()` 헬퍼 신설 — COLLECT_STATUS done/error 페이로드에 `documents[]`(doc_type/filename_dart/filename_original/file/renamed/collected/manual_upload_required/indexed/notes_count/detected_unit), `fs_divs`(CFS/OFS), `path` 추가. 기존 필드 전부 보존(무회귀).
  - 수집 여부는 `*_collected` 플래그가 아닌 **디스크 실파일 기준**(`pdf_path().exists()`) — 무클로버 재수집 시 플래그=False여도 정확히 표시.
  - **합성 항목 보강**: meta.documents[]는 이번 런에서 새로 받은 문서만 담음(무클로버 스킵분 누락) → 디스크에 작업본이 있는 doc_type은 합성해 인덱싱 현황까지 표시. (e2e에서 실제 발견한 갭 — 신한/2025Q3 재수집 시 review가 "미수집"으로 나올 뻔함.)
  - `src/static/index.html`: `renderCollectDetail()`/`reportDocLabel()` 신설, `pollCollectP` done/error 분기 교체(구버전 페이로드 폴백 유지), `doAddCollect` 요약줄+결과 누적 컨테이너 분리, `closeAddModal`에 loadLibrary 추가. 전 출력 `escapeHtml` 적용.
- **변경 파일**: `src/app.py`, `src/static/index.html`, 신규 `tests/test_collect_status_details.py`(6건).
- **검증**:
  - 단위테스트 신규 6건(renamed 판정·manual_upload·무클로버 폴백·빈 meta·fs_divs 패스스루·디스크 합성) 포함 전체 **123 passed / 실패 0**.
  - **e2e(실서버 8021)**: 신한/2025Q3 재수집 → done 페이로드에 documents 2건(report: report.pdf·주석 46 / review: 합성·review.pdf·주석 30), fs_divs=[CFS,OFS], path 확인.
  - **UI(브라우저)**: 모달에 상세 테이블 렌더 확인 — 3종 문서 구분 라벨·원본/저장 파일명·"(원본명에서 변경)"·주석 수·XBRL 연결/별도·저장경로·등록 확인 줄 모두 정상.
- **결정 사항**:
  - 저장경로는 절대경로 노출 — 에러 메시지가 아닌 사용자 요구 기능(보안 규칙의 "에러에 내부 구조 비노출"과 무충돌).
  - report 라벨은 기간 기반 파생(FY/Q2/Q1·Q3) — DART report_nm은 헤더에 별도 표기되므로 라벨은 분류 용도.
- **미완료/다음 작업**:
  - 커밋 보류분 정리: 이번 기능 `feat:` + 기존 보류분(임베딩 차원 하드닝 `fix:`, VERSION `chore:`, 개발노트 `docs:`) 분리 커밋 — 사용자 기능 테스트 후 진행.
  - VERSION: 기능 추가 → 커밋 시 0.2.1 → 0.3.0 (MINOR) 반영 검토.
  - (이월) annual(FY) 본문 PDF 자동 다운로드 실패 — 수동 업로드 대응. 이번 세션에서도 review 첨부 다운로드 실패 1건 관찰(rcept 20251114002208) — 무클로버·합성 표시로 UX는 보완됨.
- **이슈/특이사항**:
  - PowerShell 5.1 `Invoke-RestMethod`의 UTF-8 JSON 디코딩 깨짐은 콘솔 한정(서버 응답·브라우저 정상).
  - preview 스크린샷 캡처 타임아웃(환경 이슈) — 접근성 스냅샷·innerText로 대체 검증.

## 2026-06-12 작업 내역 (2차) — 수집 문서 선택 + 프리셋 기본 해제

- **작업 내용**:
  1. **수집 문서 선택** — 모달의 "본문 PDF 포함" 단일 체크박스를 3종 선택으로 확장:
     📄 보고서 본문 / 🏢 별도재무제표 검토보고서 / 🔗 연결재무제표 검토보고서 (기본 전체 체크,
     재무데이터·XBRL은 항상 수집). 선택된 문서만 DART 첨부 수집·자동 인덱싱.
  2. **프리셋 적용 문서 기본 해제** — 사업보고서 기본 체크 제거(아무것도 체크 안 됨).
     체크 없이 프리셋 클릭 시 기존 폴백(사업보고서 기준) 동작은 유지.
- **구현**:
  - `src/collect_dart.py` `collect_company()`: `collect_review`/`collect_review_sep` 파라미터 추가
    (기본 True — 기존 호출부 무회귀). 둘 다 미선택이면 `attach_docs` 네트워크 호출 자체 생략.
  - `src/app.py`: `CollectPayload`에 `include_review`/`include_review_sep`(기본 True) 추가,
    수집 파이프라인은 3개 불리언을 `include` dict 로 묶어 전달(매개변수 3개 초과 규칙).
    자동 인덱싱 루프도 선택된 문서유형만 수행(미선택 문서의 불필요한 재인덱싱 방지).
  - `src/static/index.html`: 모달 체크박스 3종(`add-report`/`add-review-sep`/`add-review`),
    `startCollect(company, period, docSel, statusEl)` 시그니처 변경(docSel 객체),
    프리셋 `preset-doctype` report `checked` 제거.
- **변경 파일**: `src/collect_dart.py`, `src/app.py`, `src/static/index.html`,
  신규 `tests/test_collect_doc_selection.py`(4건).
- **검증**:
  - 단위테스트 신규 4건(연결만/별도만/모두 해제 시 attach_docs 생략/기본 양쪽 수집) 포함
    전체 **127 passed / 실패 0**.
  - **UI e2e(브라우저)**: 프리셋 3종 기본 해제 확인. 모달에서 검토 2종 해제 후 신한·2025Q3
    수집 → `indexed=report`만(검토 수집·인덱싱 생략), 상세 테이블 정상 표시 확인.
- **결정 사항**:
  - 수집 문서 체크박스 기본값은 전체 체크(기존 동작 보존 — 명시적으로 빼는 opt-out).
  - 상세 테이블의 "인덱싱" 컬럼은 **이번 수집 런 기준**(미선택 문서는 디스크에 있어도 "–").
    영구 인덱싱 상태는 매트릭스 pill 이 표시.
- **미완료/다음 작업**: 1차 작업 항목과 동일(분리 커밋 — 사용자 기능 테스트 후).

## 2026-06-12 작업 내역 (3차) — 미수집 문서 pill 클릭 → 수집 팝업 프리셋

- **작업 내용**: 매트릭스에서 미수집/미인덱싱 문서 pill("–" 표시, 예: 신한·2025Q2 별도재무제표)을
  클릭하면 "분기/연간 자료 추가" 팝업을 열고 **회사·연도·보고서 유형을 자동 셋팅** + **해당 문서만
  수집 문서로 선택** — 사용자는 [DART 수집]만 누르면 누락 문서를 수집.
- **구현** (`src/static/index.html` 만):
  - 기존 `onEmptyCellClick` 의 모달 프리셋 로직을 `presetAddModal(company, period)` 공용 헬퍼로 추출.
  - 신규 `onAbsentDocClick(company, period, docType)`: 프리셋 + 수집 문서 체크박스를 클릭한
    문서만 체크 + 안내 배너(문서 라벨은 기간 인지 — FY=사업보고서(본문) 등).
  - `renderMatrix` 의 absent pill 에 onclick·cursor:pointer·툴팁("클릭하면 DART 수집 팝업") 추가.
    `event.stopPropagation()` 으로 셀 핸들러 간섭 차단.
- **검증** (브라우저 e2e):
  - 신한·2025Q2 "별도재무제표 –" 클릭 → 모달 오픈, 회사=신한/연도=2025/유형=Q2,
    체크박스 review_sep 만 선택, 배너 정상.
  - 신한·2025FY "사업보고서 –" 클릭 → 유형=FY, report 만 선택, 라벨 "📄 사업보고서(본문)".
  - 콘솔 에러 0건. 백엔드 무변경(테스트 추가 불필요 — 127 passed 유지).
- **결정 사항**: pill 클릭은 해당 문서만 체크(누락분 타깃 수집). 빈 셀 클릭은 기존처럼
  체크박스 상태를 건드리지 않음(셀 전체 수집 의도).

## 2026-06-12 작업 내역 (4차) — 수집 완료 후 "확인" 버튼

- **작업 내용**: 수집·인덱싱 완료 요약줄("완료 — N/M개 성공")에 **[확인] 버튼** 추가 —
  클릭 시 모달을 닫고 라이브러리 매트릭스를 즉시 갱신(`closeAddModal` 내 `loadLibrary`)해
  반영된 등록 현황을 바로 확인 가능.
- **구현**: `src/static/index.html` `doAddCollect` 종료 요약줄에 버튼 1개(기존 closeAddModal 재사용).
- **검증** (브라우저 e2e): 문서 전체 해제 수집(재무데이터만, `indexed=[]` — 게이팅 정상) →
  완료 요약 + 확인 버튼 표시 → 클릭 → 모달 닫힘·매트릭스 갱신 확인. 콘솔 에러 0건.

## 2026-06-12 작업 내역 (5차) — 수집 실패 사유 전파 + "이번 수집/기존 보유" 구분

- **배경 (사용자 리포트 진단)**: "별도재무제표를 클릭했는데 `[신한지주]분기보고서(2025.11.14).pdf`
  (사업보고서)가 수집되었다" → 조사 결과 **picker는 정상**(별도 후보 `2025.11.14 분기검토보고서`를
  정확히 선택). 실체는 ① 해당 첨부 PDF의 DART 다운로드가 반복 실패(`pdf.do` 무응답)했는데
  사유가 UI에 전혀 없음, ② 상세표가 6/1에 수집된 기존 본문 파일을 "수집 ✓"로 표시해
  이번 런 결과로 오인됨. **문서 매핑 자체는 정상**: report=공시 본문 PDF,
  review_sep=첨부 중 검토/감사+연결 미포함, review=연결+검토/감사.
- **구현**:
  - `src/collect_dart.py`: `_fetch_attachment_pdf`/`fetch_report_attachment`에
    `fail_reasons: dict` 파라미터(기존 None 반환 계약 불변) — 후보 없음/첨부 조회 실패/
    다운로드 실패/비PDF/예외/별도=연결 중복을 사유 문자열로 기록(`_note_fail`).
    `collect_company`가 선택 문서의 사유를 모아 `meta["fetch_failures"]` 저장.
    `download_attachment` 재시도 3→5회, 백오프 1.5s→2.0s 배수.
  - `src/app.py`: `_collect_details`에 `fetch_failures` 전파. `_doc_detail`에
    `existing` 필드 — 이번 런 fetch 여부를 meta 항목의 file/filename_dart 존재로 판정
    (무클로버 meta 항목·합성 항목 모두 `existing=True`). COLLECT_STATUS done/error에
    `requested`(사용자 선택 문서) 포함.
  - `src/static/index.html` `renderCollectDetail`: 수집 컬럼 `✓ 신규`(이번 런) /
    `보유`(기존 파일, 회색+툴팁) / `✗ {실패 사유}`(요청·실패, 적색) /
    `수집 대상 아님(선택 안 함)` / `미수집` 분기. 요청 문서 실패 시 헤드라인
    `⚠ ... 수집 완료 — 일부 문서 실패`.
- **변경 파일**: `src/collect_dart.py`, `src/app.py`, `src/static/index.html`,
  `tests/test_collect_doc_selection.py`(+4), `tests/test_collect_status_details.py`(+2).
- **검증**:
  - 단위테스트 신규 6건 포함 전체 **133 passed / 실패 0**.
  - **브라우저 e2e(사용자 시나리오 재현)**: 신한·2025Q3 별도 pill 클릭 → 수집 →
    `⚠ 수집 완료 — 일부 문서 실패`, 별도 행 `✗ PDF 다운로드 실패(DART 응답 없음 — 재시도 권장)`,
    본문/연결 행 `보유` 표시 — 오인 원인 완전 해소.
- **미해결(이월)**: 신한 2025Q3 `분기검토보고서` 첨부는 5회 재시도로도 다운로드 실패 지속
  (DART pdf.do 측 이슈로 추정). 후속: `odr.download` 폴백 또는 응답 상태코드 진단 필요.
  현재는 사유 안내 + PDF 업로드 탭 수동 업로드로 대응 가능.

## 2026-06-12 작업 내역 (6차) — 실패 행 시각 강조 + DART 직접 다운로드 링크

- **작업 내용**: 수집 실패 시 상세표 행을 명확히 인지되도록 강조(행 배경 적색 틴트 +
  `✗ 실패` 배지)하고, 사용자가 브라우저에서 직접 받을 수 있는 링크 2종 제공:
  **⬇ DART에서 직접 다운로드**(실패한 첨부의 실제 pdf.do URL) + **공시 원문 열기 ↗**
  (DART 뷰어) + "받은 파일은 'PDF 업로드' 탭에서 등록" 안내.
- **구현**:
  - `src/collect_dart.py`: `fetch_failures` 값이 문자열 → `{reason, url}` dict 로 확장.
    실패 지점별 URL — 다운로드 실패/비PDF=해당 첨부의 pdf_url, 후보 없음/조회 실패/예외=
    공시 뷰어(`dart_viewer_url(rcept_no)`, 신규 헬퍼 — API 키 미포함 공개 링크만).
  - `src/app.py` `_collect_details`: `viewer_url` 필드 추가(데이터로 전달).
  - `src/static/index.html`: 실패 행 강조 렌더 + 링크(target=_blank, rel=noopener).
    **외부 호스트 하드코딩 금지(폐쇄망 게이트 `test_no_external_cdn`) 준수** — 뷰어 URL 을
    프론트에서 조립하지 않고 백엔드 데이터(`viewer_url`)로만 수신(게이트에 걸려 1회 수정).
- **변경 파일**: `src/collect_dart.py`, `src/app.py`, `src/static/index.html`,
  테스트 2개 파일 dict 계약 갱신(+url 검증).
- **검증**: 전체 **133 passed / 실패 0** (폐쇄망 게이트 포함). 브라우저 e2e(별도 선택
  신한·2025Q3 수집) → `✗ 실패` 배지 + 사유 + 실제 다운로드 링크
  (`...pdf.do?rcp_no=20251114002208&dcm_no=10882571`) + 뷰어 링크 표시 확인. 콘솔 에러 0건.
- **특이사항**: 폐쇄망 데모 환경에서는 DART 링크가 열리지 않으나(외부망 차단) 링크는
  사용자 클릭 시에만 동작하는 안내용 — 자동 fetch 자원 아님(게이트 취지 부합).

## 2026-06-12 작업 내역 (7차) — pdf.do 빈 응답 근본 원인 해결 (다운로드 실패 종결)

- **진단 (라이브 실측)**: `pdf.do` 다운로드 실패의 실체는 차단/레이트리밋이 아니라
  **HTTP 200 + 빈 본문(content-length: 0)**. 변형 요청 매트릭스로 조건 특정:
  - 일반 Referer(`dart.fss.or.kr/`) / 뷰어 Referer / 쿠키 세션 → 전부 200+빈 본문
  - **Referer = 해당 문서의 다운로드 페이지(`/pdf/download/main.do?rcp_no=&dcm_no=`)
    → PDF 정상 수신**(별도 390KB, 연결 663KB). 별도·연결 모두 동일 — 6/1 성공 이후
    DART 측 정책 변경으로 추정.
- **수정** (`src/collect_dart.py`):
  - 신규 `_dart_download_referer(url)`: pdf.do URL 쿼리(rcp_no/dcm_no)에서 문서별
    다운로드 페이지 Referer 도출(파라미터 없으면 호스트 루트 폴백).
  - `download_attachment`: 고정 Referer → 문서별 Referer 적용 + dart 호스트 http URL
    https 승격.
- **검증**:
  - 단위테스트 +2(Referer 도출/폴백) — 전체 **135 passed / 실패 0**.
  - **브라우저 e2e(원래 실패 시나리오 그대로)**: 신한·2025Q3 별도 pill 클릭 → 수집 →
    `🏢 별도재무제표 검토보고서 ✓ 신규 — [신한지주]분기검토보고서(2025.11.14).pdf →
    review_sep.pdf, 주석 23개 인덱싱`. 확인 버튼 → 매트릭스 pill `별도재무제표 23` 반영.
- **이슈 종결**: 6/10 이월 "PDF 자동 다운로드 실패"의 근본 원인 해결 — annual(FY) 케이스도
  동일 메커니즘이면 함께 해소 예상(차기 FY 수집 시 확인).

## 2026-06-12 작업 내역 (8차) — PDF 업로드 감지 배너 스테일 버그 수정

- **버그 (사용자 리포트)**: 업로드 탭에서 기간=Q2 선택 상태인데 "감지 결과가 선택과
  일치합니다 (… 2025Q3 …)" 표시 → Q3 문서가 Q2 셀로 업로드됨.
- **원인**: 감지 배너가 **파일 선택 시점에만** 렌더링되고, 이후 회사/연도/기간/문서유형
  셀렉트를 바꿔도 재검증하지 않음(스테일 배너). 업로드 직전 불일치 가드도 없었음.
  감지 로직 자체(11월 분기검토보고서→2025Q3·별도)는 정상.
- **수정** (`src/static/index.html`):
  - diff 계산을 `detectDiffs(det)` 공용 헬퍼로 추출(배너·가드 공유).
  - `up-company/up-year/up-type/up-doctype` change 리스너 — 선택 변경 시 배너 즉시 재검증.
  - `doUpload` 가드: 감지·선택 불일치 시 confirm("현재 선택대로 업로드할까요? …") —
    취소하면 업로드 중단, 배너 [자동 채움]으로 보정 유도.
- **검증** (브라우저 e2e): 파일 선택(Q3, "일치" 배너) → 기간 Q2 변경 → 배너가
  "🔎 기간 2025Q2→2025Q3 [자동 채움]"으로 즉시 전환. doUpload 호출 → confirm 노출,
  취소 시 업로드 차단. [자동 채움] → 기간 Q3 복원. 콘솔 에러 0건.
  전체 pytest 135 passed 유지(백엔드 무변경).
- **후속 조치 필요(데이터)**: 버그 재현 과정에서 신한·2025Q2 별도재무제표 셀에
  Q3 문서([신한지주]분기검토보고서(2025.11.14))가 잘못 인덱싱됨(주석 23개) —
  매트릭스에서 해당 pill ✕ 로 삭제 후 올바른 반기(2025.08.14) 검토보고서 재등록 권장.
