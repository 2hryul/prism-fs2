# Spec (EARS) — 사업보고서(report) 재도입

> 2026-06-19 · prism-fs · 방안 A(현행 doc_type 모델에 report 추가)

## 문서유형·데이터 모델
- **U1** The system SHALL `report`(사업보고서)를 `review`·`review_sep`과 함께 유효 문서유형(`VALID_DOC_TYPES`)으로 인식한다.
- **U2** The system SHALL report 문서를 `report.pdf`(작업본)·`index_report.json`(주석 인덱스)·본문 인덱스(body) 경로로 관리한다.
- **U3** The system SHALL 카탈로그 엔트리에 `report_indexed`·`report_notes_count`·`report_collected` 필드를 보유한다.
- **U4** The system SHALL report 셀의 파생 fs_div를 `all`(연결·별도 중립)로 처리한다.

## 업로드·자동감지
- **E1** WHEN 사용자가 업로드 화면에서 문서유형 `사업보고서`를 선택해 PDF를 올리면 THE system SHALL 해당 PDF를 report.pdf로 저장하고 본문+주석을 인덱싱한다.
- **E2** WHEN 파일명/본문이 사업보고서 패턴(사업보고서·분기보고서·반기보고서 포함 & 검토·감사 미포함)이면 THE system SHALL 문서유형을 `report`로 자동감지·제안한다.
- **U5** The system SHALL 자료추가 모달과 업로드 드롭다운에서 사업보고서를 선택 가능한 문서유형으로 제공한다.

## 인덱싱·검색
- **E3** WHEN report 문서가 인덱싱되면 THE system SHALL 전 페이지를 본문 유닛(`no="B…"`)으로 인덱싱하고 주석과 병합해 RAG 후보에 포함한다.
- **E4** WHEN 자연어질의가 report 셀을 포함하면 THE system SHALL fs_div 필터를 우회(`all`)하여 연결·별도 구분 없이 검색한다.
- **U6** The system SHALL 주석 우선 정렬(S1, `BODY_RANK_PENALTY`)을 report 본문 유닛에도 동일 적용한다.

## 라이브러리 화면(UI)
- **U7** The system SHALL 라이브러리 매트릭스 셀에서 report 보유 시 사업보고서 pill(아이콘·주석수)을 표시한다.
- **E5** WHEN 사용자가 report pill을 클릭하면 THE system SHALL 그 문서를 비교 대상에 추가한다.
- **U8** The system SHALL 프리셋/필터의 문서유형 선택지에 사업보고서를 포함한다.

## 하위호환·제약
- **U9** The system SHALL report 미보유 기존 셀(review/review_sep만)을 종전과 100% 동일하게 동작시킨다(순수 추가).
- **IF** report PDF 파싱·인덱싱이 실패하면 **THEN** THE system SHALL 함수명·동작·원인을 포함한 에러를 반환하고 다른 문서유형 데이터를 보존한다.
- **U10** The system SHALL report 본문 RAG에 대해 인용 강제·숫자 무생성·폐쇄망(외부 https 0) 원칙을 유지한다.

## 검증
- **U11** The system SHALL 기존 pytest(160) 통과를 유지하고 report doc_type 단위 테스트(경로·검출·카탈로그 필드·fs_div=all 라우팅)를 추가한다.
